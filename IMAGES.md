# Imágenes utilizadas en la landing

- /public/images/clinic-hero1.webp — Real (archivo del cliente, tamaño optimizado a WebP < 200 KB).
- /public/images/clinic-hero2.webp — Real (archivo del cliente, tamaño optimizado a WebP < 200 KB).
- /public/images/clinic-hero3.webp — Real (archivo del cliente, tamaño optimizado a WebP < 200 KB).
- /public/og-default.jpg — Generada para OG (placeholder; deberá reemplazarse por una imagen real de 1200x630).
- /public/images/ assets del cliente (referencia de origen) — Cumple con formato WebP y tamaño < 200 KB.

Notas:
- Se deben respetar las políticas de accesibilidad WCAG 2.2 AA para contraste y lectura.
- Todos los assets deben estar en public/ y optimizados (WebP < 200KB).